# Predictive-SHM Backend — 以 TensorFlow 2.8 (tf28) 为主，torch 可选
# 已按 conda 环境 tf28（Python 3.9、TF 2.8.4）对齐版本范围
# 完全使用 FastAPI，无 Flask 依赖

# FastAPI 服务
fastapi>=0.100.0,<0.116.0
uvicorn[standard]>=0.22.0,<0.40.0
pydantic>=2.0.0,<2.10.0
python-multipart>=0.0.6

# 模型推理：TensorFlow 2.8（与 tf28 环境一致）
tensorflow>=2.8.0,<2.9.0

# 数值与数据处理（与 tf28 环境：numpy 1.26 / pandas 2.3 / scikit-learn 1.6 兼容）
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.2.0

# HTTP
requests>=2.28.0

# ---------- 可选：仅当使用 .pth 或 prediction_service.py 内置 PyTorch 推理时安装 ----------
# pip install "torch>=2.0.0"

# ---------- 可选：仅当使用 ONNX 模型（user_models 中的 .onnx）时安装 ----------
# pip install onnxruntime

# ---------- 可选：MQTT 桥接脚本（scripts/mqtt_bridge.py）------------
# pip install paho-mqtt
